
const SectionHeading = ({title}) => {
  return (
    <div className="my-10">
      <h3 className="text-3xl font-semibold border-b-2 border-t-2 border-primary w-fit mx-auto p-2">{title}</h3>
    </div>
  );
};

export default SectionHeading;